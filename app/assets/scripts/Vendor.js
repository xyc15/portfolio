//we normally import packages like lazysizes in a separate javascript file. These packages can run on their own without us needing to write any supplyment code.
import 'lazysizes';
import 'picturefill';
import '../../temp/scripts/modernizr'
